# General purpose succinct server .
It is an empty application that just depends on succinct collab plugin. Can be used as a oauthserver or simple utility applications that just need the collab plugin.
